const baseURL = 'http://localhost:10000';

// Enum for options
const metricType = {
    FOLLOWERS: 'followers',
    ACTIVITY: 'activity',
    NONE: 'none',
};

const stylingType = {
    SIZE: 'size',
    COLOR: 'color',
};

const defaultNodeColor = '#666666';
const defaultNodeSize = 15;

export { baseURL, metricType, stylingType, defaultNodeColor, defaultNodeSize };
